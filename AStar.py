from Node import Node

class AStar:
	def __init__(self, Start, Goal, Nodes):
		self.Start = Start
		self.Goal = Goal
		self.Nodes = Nodes
		openList = []
		
	def Step1(self):
		openList.append(Start)
		
